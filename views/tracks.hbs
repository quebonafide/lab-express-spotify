<div>
    <ul>
        {{#each data.body.items}}    
            <li> 
                {{this.name}}
                {{#if this.preview_url}}
                  <audio controls>
                  <source src="{{this.preview_url}}" type="audio/mpeg">
                </audio>
                {{/if}}
        {{/each}}
    </ul>
</div>